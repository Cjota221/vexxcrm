export interface MetaAdAccount {
  id: string;
  tenant_id: string;
  nome: string;
  ad_account_id: string; // "act_XXXXXXXXX"
  ativa: boolean;
  cor: string;
  ordem: number;
  created_at: string;
  updated_at: string;
}
